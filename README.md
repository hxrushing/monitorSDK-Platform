# 📊 前端数据分析与监控平台

一个企业级的前端数据采集、分析和可视化平台，提供完整的埋点SDK、数据管理后台、AI智能分析和时序预测功能。

## ✨ 核心特性

### 🎯 可插拔探针SDK
- **多探针支持**：错误监控、性能监控、HTTP请求监控、用户行为追踪
- **批量发送机制**：智能批量上报，减少网络请求98%
- **离线缓存**：网络断开时自动存储，恢复后自动上报
- **指数退避重试**：智能重试机制，支持网络感知和错误类型感知
- **数据压缩**：自动压缩传输数据，减少60-80%带宽消耗
- **Beacon API**：页面关闭时确保数据上报，不阻塞页面卸载

### 📈 数据可视化
- **实时数据统计**：PV、UV、转化率等核心指标实时展示
- **多维度分析**：事件分析、漏斗分析、趋势对比
- **性能监控**：Web Vitals指标可视化（FCP、LCP、CLS、TTFB、INP）
- **LTTB算法**：大数据量降采样，保持趋势特征的同时提升渲染性能
- **交互式图表**：基于Ant Design Charts的丰富图表组件

### 🤖 AI智能分析
- **智能数据总结**：基于OpenAI的自动数据分析报告
- **实时进度跟踪**：SSE实时推送AI总结生成进度
- **定时报告**：每日自动生成并邮件发送分析报告
- **Token优化**：智能Token估算和分批处理，支持大数据量

### 🔮 时序预测
- **ML服务集成**：基于LSTM/GRU的深度学习预测模型
- **多指标预测**：支持PV、UV、转化率等指标的未来趋势预测
- **自定义周期**：支持1-30天的预测周期
- **预测历史**：完整的预测记录和对比分析

### 🏗️ 企业级功能
- **多项目支持**：支持多项目隔离和权限管理
- **用户体系**：完整的用户注册、登录、权限管理
- **RSA加密登录**：前端RSA加密，后端私钥解密，保障登录安全
- **JWT认证**：无状态身份验证，支持Token刷新
- **项目管理**：项目创建、配置、SDK下载一体化

## 🏗️ 项目架构

```
sdk-platform/
├── src/                    # 前端管理平台
│   ├── components/        # React组件
│   ├── pages/             # 页面组件
│   ├── sdk/               # SDK核心代码
│   │   ├── core/          # 核心模块（API层、传输管道）
│   │   ├── probes/        # 探针模块（错误、HTTP、性能、行为）
│   │   ├── config/        # 配置系统
│   │   └── utils/         # 工具函数
│   ├── hooks/             # React Hooks
│   ├── services/          # API服务
│   └── utils/             # 工具函数
├── server/                 # 后端服务（Node.js + Express）
│   ├── src/
│   │   ├── routes/        # API路由
│   │   ├── services/      # 业务逻辑层
│   │   ├── database/      # 数据库访问层
│   │   └── utils/         # 工具函数
│   └── dist/              # 编译输出
├── ml-service/            # ML预测服务（Python + Flask）
│   ├── app.py             # Flask应用入口
│   └── requirements.txt    # Python依赖
└── docs/                  # 项目文档
```

## 🚀 快速开始

### 环境要求

- **Node.js**: >= 18.0.0
- **Python**: >= 3.8 (用于ML服务)
- **MySQL**: >= 5.7
- **pnpm**: >= 8.0 (推荐使用pnpm)

### 安装步骤

#### 1. 克隆项目

```bash
git clone https://github.com/your-username/sdk-platform.git
cd sdk-platform
```

#### 2. 安装前端依赖

```bash
pnpm install
```

#### 3. 安装后端依赖

```bash
cd server
pnpm install
cd ..
```

#### 4. 配置环境变量

**后端环境变量** (`server/.env`):
```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=analytics_db

# JWT配置
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d

# RSA密钥（用于登录加密）
RSA_PRIVATE_KEY=your_rsa_private_key

# OpenAI配置（用于AI总结功能）
OPENAI_API_KEY=your_openai_api_key

# ML服务地址
ML_SERVICE_URL=http://localhost:5000

# 邮件配置（可选，用于定时报告）
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_password
```

**ML服务环境变量** (`ml-service/.env`):
```env
FLASK_ENV=production
FLASK_DEBUG=False
PORT=5000
```

#### 5. 初始化数据库

```bash
cd server
pnpm run init-db
```

#### 6. 启动服务

**启动后端服务**:
```bash
cd server
pnpm run dev  # 开发环境
# 或
pnpm run build && pnpm start  # 生产环境
```

**启动ML服务**:
```bash
cd ml-service
pip install -r requirements.txt
python app.py
```

**启动前端开发服务器**:
```bash
pnpm dev
```

访问 `http://localhost:5173` 查看前端管理平台。

## 📦 SDK使用

### 方式一：ESM模块引入（推荐）

```typescript
import { init } from './libs/sdk';

const sdk = init({
  projectId: 'your-project-id',
  endpoint: 'https://your-api.com/api/track',
  enable: {
    error: true,      // 启用错误监控
    http: true,       // 启用HTTP请求监控
    perf: true,       // 启用性能监控
    behavior: true,   // 启用行为监控
  },
  batch: {
    maxBatchSize: 50,
    flushInterval: 5000,
    enableOfflineStorage: true,
  },
});

// 发送自定义事件
sdk.track('page_view', { page: '/home' });

// 发送错误事件
sdk.trackError('api_error', { message: 'Request failed' });

// 手动刷新队列
await sdk.flush();
```

### 方式二：通过Script标签引入

```html
<script src="https://cdn.example.com/sdk/index.global.js"></script>
<script>
  const sdk = Analytics.init({
    projectId: 'your-project-id',
    endpoint: 'https://your-api.com/api/track',
  });
  
  sdk.track('page_view', { page: '/home' });
</script>
```

### SDK构建

```bash
# 构建SDK
pnpm run build:sdk

# 构建产物在 dist/sdk/ 目录
# - index.js (ESM格式)
# - index.cjs (CommonJS格式)
# - index.global.js (IIFE格式，用于script标签)
# - index.d.ts (TypeScript类型定义)
```

## 🔧 配置说明

### SDK配置选项

```typescript
interface SDKConfig {
  projectId: string;                    // 项目ID（必填）
  endpoint: string;                     // 上报端点（必填）
  enable?: {
    error?: boolean;        // 错误探针 (默认: true)
    http?: boolean;         // HTTP探针 (默认: true)
    perf?: boolean;         // 性能探针 (默认: true)
    behavior?: boolean;     // 行为探针 (默认: true)
  };
  sampleRate?: {
    perf?: number;         // 性能指标采样率 (0-1, 默认: 1.0)
    http?: number;         // HTTP请求采样率 (0-1, 默认: 1.0)
    error?: number;        // 错误采样率 (0-1, 默认: 1.0)
  };
  batch?: {
    maxBatchSize?: number;        // 最大批量大小 (默认: 50)
    flushInterval?: number;        // 刷新间隔毫秒 (默认: 5000)
    maxRetries?: number;          // 最大重试次数 (默认: 3)
    enableOfflineStorage?: boolean; // 是否启用离线存储 (默认: true)
    exponentialBackoff?: {
      enabled?: boolean;            // 是否启用指数退避 (默认: true)
      baseDelay?: number;           // 基础延迟毫秒 (默认: 1000)
      maxDelay?: number;            // 最大延迟毫秒 (默认: 30000)
    };
  };
  debug?: boolean;                      // 调试模式
}
```

## 📚 文档

- [SDK接入说明](./docs/SDK接入说明.md)
- [核心SDK模块文档](./核心SDK模块文档.md)
- [后端服务功能说明](./Server-Backend服务功能说明文档.md)
- [ML服务功能说明](./ML-Service功能说明文档.md)
- [批量发送机制说明](./src/sdk/README.md)
- [可插拔架构说明](./src/sdk/README_PLUGGABLE.md)
- [部署指南](./docs/快速部署指南.md)

更多文档请查看 [docs](./docs/) 目录。

## 🎯 核心功能详解

### 批量发送机制

- **智能批量**：自动将多个事件合并为一次网络请求
- **优先级队列**：高优先级事件（如错误）优先发送
- **自适应批量大小**：根据网络状况和队列长度动态调整
- **性能提升**：网络请求减少98%，带宽节省60-80%

### 指数退避重试

- **真正的指数退避**：使用 2^n 指数增长
- **抖动机制**：添加随机抖动避免雷群效应
- **网络感知**：根据网络状况动态调整退避时间
- **错误类型感知**：根据错误类型选择不同的退避策略

### 数据压缩

- **智能压缩**：自动选择最佳压缩算法
- **数据去重**：提取公共字段，减少重复数据
- **字典压缩**：使用短标识符替换重复字符串
- **压缩效果**：通常可减少60-80%的数据传输量

### AI智能分析

- **自动数据总结**：基于OpenAI GPT模型生成数据分析报告
- **实时进度跟踪**：SSE实时推送生成进度
- **Token优化**：智能Token估算，支持大数据量分批处理
- **定时报告**：每日自动生成并邮件发送

### 时序预测

- **深度学习模型**：基于LSTM/GRU的时序预测
- **多指标支持**：PV、UV、转化率等指标预测
- **自定义周期**：支持1-30天的预测周期
- **预测历史**：完整的预测记录和对比分析

## 🛠️ 技术栈

### 前端
- **框架**: React 18.2 + TypeScript 5.0
- **构建工具**: Vite 4.4
- **UI组件库**: Ant Design 5.0
- **图表库**: @ant-design/plots
- **状态管理**: Zustand
- **路由**: React Router 7.3

### 后端
- **框架**: Express.js + TypeScript
- **数据库**: MySQL 5.7+
- **认证**: JWT + RSA加密
- **缓存**: LRU内存缓存
- **定时任务**: Node-Cron

### ML服务
- **框架**: Flask
- **深度学习**: TensorFlow/Keras
- **机器学习**: Scikit-learn
- **数据处理**: Pandas/NumPy

## 📊 性能指标

- **SDK体积**: < 50KB (gzipped)
- **批量发送**: 网络请求减少98%
- **数据压缩**: 带宽节省60-80%
- **离线存储**: 支持1MB离线缓存
- **重试机制**: 智能指数退避，最大延迟30秒

## 🔒 安全特性

- **RSA加密登录**：前端RSA加密，后端私钥解密
- **JWT认证**：无状态身份验证，支持Token刷新
- **密码安全**：SHA-256+盐值哈希
- **数据脱敏**：HTTP探针支持敏感字段脱敏
- **CORS配置**：支持跨域请求配置

## 🤝 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📮 联系方式

如有问题或建议，请通过以下方式联系：

- 发送邮件至: 1638803356@qq.com

---

⭐ 如果这个项目对你有帮助，请给个 Star 支持一下！
